# GitHub personal access token with repo scope
GH_TOKEN=ghp_your_token_here
# Owner and repo for the target project repo the agents will work on
GH_OWNER=your-gh-username-or-org
GH_REPO=sample-python-app
# Clone URL for the project repo (HTTPS with token or SSH)
PROJECT_CLONE_URL=https://github.com/${GH_OWNER}/${GH_REPO}.git
# Optional: default branch name if API lookup fails
DEFAULT_BRANCH=main

# Working directory for local clones
WORKDIR_ROOT=/tmp/codex-work

# --- Ollama via LiteLLM (optional; MVP runs without it) ---
OLLAMA_BASE_URL=http://aether:11434

# Per-role model defaults (used when LLM is enabled)
LLM_PLANNER_MODEL=qwen3:32b
LLM_TASKMASTER_MODEL=qwen3:32b
LLM_DEVELOPER_MODEL=qwen3-coder:latest
LLM_REVIEWER_MODEL=qwen3:32b
LLM_INTEGRATOR_MODEL=none

# Developer agent: use LLM for planning? (true/false)
DEV_USE_LLM=false

